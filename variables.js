// En var el compilador ira siempre al inicio del archivo y crea la variables con var
var miVariable = 'variable'

// en let se mantiene en la zona donde se declara la variable, es decir solo funcionará en el scope de la variable
let variable = 'variable creada con let'

const constante = 'este valor es constante'

console.log(miVariable)